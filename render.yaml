services:
  - type: web
    name: sica-tutor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
    disk:
      name: experiment-data
      mountPath: /data  # 👈 這裡填 /data
      sizeGB: 5
